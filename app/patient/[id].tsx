import { useMemo } from 'react';
import { Pressable, ScrollView, View } from 'react-native';
import { ArrowLeft, CalendarPlus, ChevronRight, ClipboardPlus, Ruler, Stethoscope } from 'lucide-react-native';
import { useLocalSearchParams, useRouter } from 'expo-router';
import { Accordion, Avatar, Badge, Button, Card, Choices, Divider, IconButton, Label, SectionTitle, Txt, s } from '../../src/ui/components';
import { colors as c, fonts } from '../../src/ui/theme';
import { age, dateLabel, number } from '../../src/domain/clinical';
import { useStore } from '../../src/data/store';
import type { Patient } from '../../src/domain/types';
export default function PatientDetail() { const { id } = useLocalSearchParams<{ id: string }>(); const router = useRouter(); const { patients, wounds, visits } = useStore(st => st.data); const presentation = useStore(st => st.presentation); const patient = patients.find(p => p.id === id); const patientWounds = wounds.filter(w => w.patientId === id); const patientVisits = visits.filter(v => v.patientId === id).sort((a, b) => b.date.localeCompare(a.date)); if (!patient) return <View style={{ flex: 1, padding: 24, backgroundColor: c.bg }}><Txt>Paciente não encontrado.</Txt></View>; return <View style={{ flex: 1, backgroundColor: c.bg }}><ScrollView contentContainerStyle={{ padding: 24, paddingTop: 16, paddingBottom: 50, gap: 20 }}><View style={s.between}><IconButton icon={ArrowLeft} label="Voltar" onPress={() => router.back()} /><View style={{ flexDirection: 'row', gap: 6 }}><IconButton icon={CalendarPlus} label="Novo atendimento" color={c.red} onPress={() => router.push(`/care/new?patientId=${id}`)} /><IconButton icon={ClipboardPlus} label="Adicionar ferida" color={c.red} onPress={() => {}} /></View></View><View style={s.row}><Avatar name={patient.name} color={patient.color} size={66} anonymous={presentation} /><View style={{ flex: 1, gap: 5 }}><Txt style={{ fontFamily: fonts.semibold, fontSize: 22 }}>{presentation ? 'Paciente em apresentação' : patient.name}</Txt><Txt muted>{presentation ? 'Identificação oculta' : `${patient.sex} · ${age(patient.birthDate)} anos · ${patient.status}`}</Txt></View></View><Card style={{ padding: 18, backgroundColor: c.redSoft, borderColor: '#F3D2D2', gap: 8 }}><View style={s.row}><Stethoscope size={18} color={c.red} /><Txt style={{ color: c.red, fontFamily: fonts.semibold }}>Prontuário profissional</Txt></View><Txt style={{ color: '#7F3030', fontSize: 12, lineHeight: 19 }}>Dados fictícios para demonstração. Conecte o Supabase para trabalhar com a conta autenticada.</Txt></Card><View style={{ gap: 10 }}><SectionTitle title="FERIDAS DO PACIENTE" action="Histórico" />{patientWounds.map(w => <Pressable key={w.id} onPress={() => router.push(`/care/new?patientId=${id}&woundId=${w.id}`)}><Card style={{ padding: 18, gap: 12 }}><View style={s.between}><View style={{ gap: 6, flex: 1 }}><Txt style={{ fontFamily: fonts.semibold, fontSize: 16 }}>{presentation ? 'Localização oculta' : w.location}</Txt><View style={s.row}><Badge tone={w.status === 'Estagnada' ? 'amber' : 'green'}>{w.status}</Badge><Badge>{w.etiology}</Badge></View></View><ChevronRight size={19} color={c.tertiary} /></View><View style={s.row}><Ruler size={15} color={c.secondary} /><Txt muted style={{ fontSize: 12 }}>Início {dateLabel(w.startDate)} · {Math.max(0, Math.floor((Date.now() - Date.parse(w.startDate)) / 86400000))} dias de evolução</Txt></View></Card></Pressable>)}</View><Accordion title="Identificação" subtitle="Dados pessoais e contato" initialOpen><FieldRow label="Nome completo" value={presentation ? 'Oculto no modo apresentação' : patient.name} /><FieldRow label="Nascimento · idade" value={presentation ? 'Oculto' : `${dateLabel(patient.birthDate)} · ${age(patient.birthDate)} anos`} /><FieldRow label="Sexo" value={presentation ? 'Oculto' : patient.sex} /><FieldRow label="CPF · Cartão SUS" value={presentation ? 'Oculto' : `${patient.cpf || 'Não informado'} · ${patient.sus || 'Não informado'}`} /><FieldRow label="Telefone" value={presentation ? 'Oculto' : patient.phone || 'Não informado'} /><FieldRow label="Endereço" value={presentation ? 'Oculto' : patient.address} /><FieldRow label="Contato de emergência" value={presentation ? 'Oculto' : patient.emergency || 'Não informado'} /></Accordion><Accordion title="Dados clínicos" subtitle="Medidas, hábitos e alergias"><View style={styles.grid}><FieldRow label="Peso" value={`${number(patient.weight)} kg`} /><FieldRow label="Altura" value={`${number(patient.height / 100)} m`} /><FieldRow label="IMC" value={`${number(patient.weight / ((patient.height / 100) ** 2))} kg/m²`} /><FieldRow label="Tipo sanguíneo" value={patient.bloodType} /></View><Divider /><Label>ALERGIAS</Label><Choices options={patient.allergies.length ? patient.allergies : ['Nenhuma registrada']} value={patient.allergies} onChange={() => {}} multiple /><FieldRow label="Tabagismo" value={patient.smoking} /><FieldRow label="Etilismo" value={patient.alcohol} /><FieldRow label="Atividade física" value={patient.activity} /></Accordion><Accordion title="Comorbidades" subtitle={`${patient.comorbidities.length} registradas`}><View style={{ gap: 12 }}>{patient.comorbidities.map(cmb => <View key={cmb.name} style={s.between}><View style={{ flex: 1, gap: 4 }}><Txt style={{ fontFamily: fonts.medium }}>{cmb.name}</Txt><Txt muted style={{ fontSize: 12 }}>Diagnóstico {dateLabel(cmb.date)}{cmb.note ? ` · ${cmb.note}` : ''}</Txt></View><Badge tone="red">ativa</Badge></View>)}</View></Accordion><Accordion title="Medicamentos em uso" subtitle={`${patient.medications.length} em uso`}><View style={{ gap: 12 }}>{patient.medications.map(med => <View key={med.name} style={s.between}><View style={{ flex: 1, gap: 4 }}><Txt style={{ fontFamily: fonts.medium }}>{med.name} · {med.dose}</Txt><Txt muted style={{ fontSize: 12 }}>{med.route} · {med.frequency} · {med.indication}</Txt></View>{med.attention && <Badge tone="amber">atenção</Badge>}</View>)}</View></Accordion><Accordion title="Exames relevantes" subtitle={`${patient.exams.length} registros`}><View style={{ gap: 12 }}>{patient.exams.map(exam => <View key={`${exam.type}-${exam.date}`} style={s.between}><View><Txt style={{ fontFamily: fonts.medium }}>{exam.type}</Txt><Txt muted style={{ fontSize: 12 }}>{exam.value} · {dateLabel(exam.date)}</Txt></View><Badge>{exam.attachment ? 'anexo' : 'sem anexo'}</Badge></View>)}</View></Accordion><View style={{ gap: 12 }}><SectionTitle title="HISTÓRICO DE ATENDIMENTOS" />{patientVisits.slice(0, 4).map(v => <Card key={v.id} style={{ padding: 16, flexDirection: 'row', alignItems: 'center' }}><View style={{ flex: 1, gap: 4 }}><Txt style={{ fontFamily: fonts.medium }}>{dateLabel(v.date)} · {v.state}</Txt><Txt muted style={{ fontSize: 12 }}>{v.length && v.width ? `${v.length} × ${v.width} cm · ${number(Number(v.length) * Number(v.width))} cm²` : 'Rascunho sem mensuração'}</Txt></View><ChevronRight size={17} color={c.tertiary} /></Card>)}</View><Button title="Iniciar atendimento" icon={CalendarPlus} onPress={() => router.push(`/care/new?patientId=${id}`)} /></ScrollView></View>; }
function FieldRow({ label, value }: { label: string; value: string }) { return <View style={{ gap: 4, flex: 1 }}><Label>{label}</Label><Txt>{value}</Txt></View>; }
const styles = { grid: { flexDirection: 'row' as const, flexWrap: 'wrap' as const, gap: 18 } };
