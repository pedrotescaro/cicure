import { ScrollView, View } from 'react-native';
import { Moon, ShieldCheck, SlidersHorizontal, UserRound } from 'lucide-react-native';
import { Card, SectionTitle, Txt, s } from '../../src/ui/components';
import { colors as c } from '../../src/ui/theme';
import { useStore } from '../../src/data/store';
export default function More() { const profile = useStore(st => st.data.profiles[0]); const presentation = useStore(st => st.presentation); const setPresentation = useStore(st => st.setPresentation); return <View style={{ flex: 1, backgroundColor: c.bg }}><ScrollView contentContainerStyle={{ padding: 24, paddingTop: 25, paddingBottom: 170, gap: 22 }}><Txt muted style={{ fontSize: 13 }}>CONFIGURAÇÕES</Txt><Txt style={s.h1}>Mais</Txt><Card style={{ flexDirection: 'row', alignItems: 'center' }}><View style={{ width: 48, height: 48, borderRadius: 18, backgroundColor: c.redSoft, alignItems: 'center', justifyContent: 'center' }}><UserRound size={21} color={c.red} /></View><View style={{ flex: 1, gap: 4 }}><Txt style={{ fontFamily: 'Inter_600SemiBold' }}>{profile?.name}</Txt><Txt muted style={{ fontSize: 12 }}>{profile?.specialty} · {profile?.council}</Txt></View></Card><SectionTitle title="PREFERÊNCIAS" /><Card style={{ gap: 0, padding: 0 }}><Row icon={SlidersHorizontal} title="Escalas e catálogos" subtitle="Versões, produtos e terapias" /><Row icon={ShieldCheck} title="Privacidade e LGPD" subtitle="Consentimentos e registro de acesso" /><Row icon={Moon} title="Modo apresentação" subtitle={presentation ? 'Ativado' : 'Ocultar identificadores ao apresentar'} onPress={() => setPresentation(!presentation)} /></Card><Txt muted style={{ fontSize: 12, textAlign: 'center', marginTop: 10 }}>Cicura · ambiente de demonstração local</Txt></ScrollView></View>; }
function Row({ icon: Icon, title, subtitle, onPress }: { icon: any; title: string; subtitle: string; onPress?: () => void }) { return <View style={{ minHeight: 74, paddingHorizontal: 20, flexDirection: 'row', alignItems: 'center', gap: 13, borderBottomWidth: 1, borderBottomColor: c.border }}><Icon size={20} color={c.secondary} /><View style={{ flex: 1, gap: 3 }}><Txt style={{ fontFamily: 'Inter_600SemiBold' }}>{title}</Txt><Txt muted style={{ fontSize: 12 }}>{subtitle}</Txt></View>{onPress && <Txt onPress={onPress} style={{ color: c.red, fontSize: 12 }}>Alternar</Txt>}</View>; }
